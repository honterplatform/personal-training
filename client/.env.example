VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
# Leave empty in production (web served from same origin as the API).
# In dev, Vite proxies /api to localhost:4000 so leaving this empty works too.
VITE_API_BASE_URL=
